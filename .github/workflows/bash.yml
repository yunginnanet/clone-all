on:
  push:
    branches:
      - master
name: "shellcheck"
jobs:
  shellcheck:
    name: shellcheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: shellcheck
        uses: ludeeus/action-shellcheck@master
